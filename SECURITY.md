# 安全问题报告

## 请不要开公开 issue

发现漏洞请走 GitHub 的私密报告通道：

仓库页 → **Security** 标签页 → **Report a vulnerability**

这条通道只有仓库维护者可见，可以在公开之前先把修复做出来。

## 这个项目值得关注的几类问题

它处理的是求职者的个人资料和岗位资格判断，所以下面这些尤其想知道：

- **越权读取他人资料。** 任何能用一个候选人的身份读到另一个候选人资料、会话或待确认项的路径。
- **绕过资格判定。** 任何能让用户看到一条没有确定性规则支撑的资格结论的路径——
  包括让模型的自由文本绕过模板与依据校验。
- **凭据或个人信息泄露。** 出现在日志、报错信息、API 响应或提交历史里的口令、令牌、
  身份证号、手机号。`ArtifactImportFailure` 有一层脱敏，绕过它的算这一类。
- **依赖链问题。** 已知 CVE 的依赖，或构建过程中引入的未签名产物。

## 已知的未修项

这些是公开已知、尚未修复的，报告它们不算新发现：

- **没有按候选人的授权。** 任何通过认证的运维账号都能对任意 `candidateId` 发起查询和运行。
  归属检查只挡住了"别人的会话"，挡不住"别人的候选人"。当前版本假定部署在单人可信环境里。
- 一个评估不出来的岗位会让整次排名查询失败。

## 响应

这是一个业余时间维护的项目，没有 SLA。我会尽量在收到后一周内给出确认，
修复时间取决于问题的严重程度和复杂度。

## 支持范围

只有默认分支（`main`）的最新提交会收到修复。没有长期支持分支。
