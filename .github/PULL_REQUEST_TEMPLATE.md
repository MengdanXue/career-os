## 这个 PR 解决什么问题

<!-- 说清楚为什么需要它。关联的 issue 写 Fixes #123。 -->

## 怎么验证的

<!--
修缺陷的话，请贴出**修复前**那条用例失败的输出，和修复后通过的输出。
"我本地跑过了"不算——具体是哪条用例、期望什么、原先实得什么。
-->

## 自检

- [ ] 每个提交都有 `Signed-off-by`（`git commit -s`，见 [CONTRIBUTING](../CONTRIBUTING.md#一开发者证书dco)）
- [ ] `mvn clean verify` 通过；跳过的用例没有变多
- [ ] 修缺陷的 PR 带了一条能复现它的用例，并且确认过它在修复前是红的
- [ ] 没有引入个人信息：真实姓名、生日、证件号、联系方式、本机绝对路径、凭据
- [ ] 改了数据库迁移的话：新增文件而非修改已发布的；同步了 `MigrationIntegrationTest` 里的迁移条数；
      动了种子事实值的话重算了 `value_fingerprint`
